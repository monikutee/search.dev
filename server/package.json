{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && NODE_ENV=development node dist/index.js",
    "start_db": "docker-compose --env-file .env.development up",
    "dev": "nodemon -w src -e ts,tsx --exec NODE_ENV=development yarn start",
    "test": "NODE_ENV=test jest",
    "start_test_db": "docker-compose -f tests-docker-compose.yml --env-file .env.test up -d",
    "test_sync": "NODE_ENV=test jest --runInBand",
    "start_docker": "node dist/index.js",
    "lint": "eslint '*/**/*.{js,ts}' --quiet --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "bcrypt": "^5.1.1",
    "dotenv": "16.0.1",
    "express": "^4.18.3",
    "joi": "^17.12.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.4",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "@typescript-eslint/parser": "^7.1.1",
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0",
    "typescript": "^5.3.3"
  }
}
